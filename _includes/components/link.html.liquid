{% assign include = include.link %}
		<link type="{{ include.link.type | default: 'text/css' }}" href="{{ include.href }}" rel="{{ include.rel | default: 'stylesheet' }}"{% if include.integrity != null %} integrity="{{ include.integrity }}"{% endif %}{% if include.crossorigin != null %} crossorigin="{{ include.crossorigin }}"{% endif %}>